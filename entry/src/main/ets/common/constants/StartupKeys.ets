/** 启动引导层 AppStorage 键（EntryAbility / AppBootstrap / Index 共享） */
export const STARTUP_SPLASH_VISIBLE_KEY: string = 'veneraStartupSplashVisible';
export const STARTUP_SPLASH_MESSAGE_KEY: string = 'veneraStartupSplashMessage';
/** QuickJS 与默认图源后台装入完成（主界面 Tab 可安全调用 JS API） */
export const ENGINE_READY_KEY: string = 'veneraJsEngineReady';
/** 发现 Tab 再次点击时滚动到顶（MainShell → ExplorePage） */
export const EXPLORE_SCROLL_TOP_KEY: string = 'veneraExploreScrollTop';
